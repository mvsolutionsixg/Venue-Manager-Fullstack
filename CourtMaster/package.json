{
    "name": "court-master-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"cd backend && uvicorn app.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && npm install --prefix frontend && cd backend && pip install -r requirements.txt"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}